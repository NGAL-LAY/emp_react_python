import React from 'react';

const Positions = () => (
  <div>
    <h2>Employee Positions</h2>
    <p>Welcome to the Positions page. Only authenticated users can see this.</p>
  </div>
);

export default Positions;
